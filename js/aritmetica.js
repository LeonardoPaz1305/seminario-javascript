let a = 5,
    b = 2,
    resultado = 0;

resultado = a + b;

resultado = a - b;

resultado = a / b;

// Modulo
resultado = a % b;

// if (a % 2 == 0) {
//     console.log('Par')
// } else {
//     console.log('impar')
// }

// Potencia (a = base y b = exponente )
resultado = a ** b; // 5 al cuadrado

//---------------------------------------
// Orden de precedencia
resultado = 2 + 2 * 5;

resultado = (2 + 2) * 5;

resultado = (2 + 2) * 5 / (2 + 3);
resultado = (4) * 5 / (5);
resultado = (20) / (5);


//---------------------------------------

a = 10;
a += 5; // a = a + 5;

a -= 5; // a = a - 5;

a *= 5; // a = a * 5;

a /= 5; // a = a * 5;

a %= 5; // a = a % 5;

a **= 5; // a = a * 5;